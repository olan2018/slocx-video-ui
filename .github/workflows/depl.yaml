name: build_deploy_aks
on:
  push:
    branches:
      - main
    paths-ignore:
      - 'Readme.md'
      - 'README.md'
      - '*.md'
      - 'docs/**'
      - '.gitignore'
      - 'LICENSE'
      - '.editorconfig'
      - '.vscode/**'
      - '.idea/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Log in to Container Registry
        run: |
          echo "${{ secrets.CR_PASSWORD }}" | docker login ${{ secrets.CR_REGISTRY }} -u ${{ secrets.CR_USERNAME }} --password-stdin

      - name: Build and Push Image
        run: |
          docker build -t ${{ secrets.CR_REGISTRY }}/video-call-slocx-frontend:${{ github.sha }} .
          docker push ${{ secrets.CR_REGISTRY }}/video-call-slocx-frontend:${{ github.sha }}

  update-helm:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout Helm charts repository
        uses: actions/checkout@v3
        with:
          repository: olan2018/helm-chart
          token: ${{ secrets.PAT_TOKEN }}
          ref: main

      - name: Update image tag in values.yaml
        run: |
          sed -i 's|tag: .*|tag: "'"${{ github.sha }}"'"|' video-call-slocx-frontend/values.yaml

      - name: Commit and push changes
        env:
          GIT_COMMITTER_NAME: "GitHub Actions"
          GIT_COMMITTER_EMAIL: "github-actions@github.com"
        run: |
          git config --global user.name "$GIT_COMMITTER_NAME"
          git config --global user.email "$GIT_COMMITTER_EMAIL"
          git add video-call-slocx-frontend/values.yaml
          git commit -m "Update video-call-slocx-frontend Prod image tag to ${{ github.sha }}" || echo "No changes to commit"
          git push origin main || echo "No changes to push"
